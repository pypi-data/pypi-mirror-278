[tool.poetry]
name = "record-batcher"
version = "0.1.0"
description = "Batch records based on size constraints"
authors = ["Krystian Jakusik <krystian.jakusik@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "record_batcher"}]

[tool.poetry.dependencies]
python = "^3.7"


[tool.poetry.group.test.dependencies]
parameterized = "^0.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
