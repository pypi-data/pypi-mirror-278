from setuptools import setup, find_packages

setup(
    name='bsp_package',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # package requirements go here
    ]
)