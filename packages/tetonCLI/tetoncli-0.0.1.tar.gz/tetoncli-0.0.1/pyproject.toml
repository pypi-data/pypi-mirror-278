[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tetonCLI"
version = "0.0.1"
authors = [
  { name="Emil Koch Ringtved", email="emil@teton.ai" },
]
description='a client for wrapping scripts used internally'
readme = "README.md"
requires-python = ">=3.8"