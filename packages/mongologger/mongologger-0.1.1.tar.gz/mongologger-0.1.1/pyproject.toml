[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mongologger"
version = "0.1.1"
dependencies = [
    "motor==3.4.0",
]

[tool.setuptools]
packages = ["mongologger"]
