[default]
linode_name = docker

[local]
containers = (esphome,webdav)

[raspi]
ssh_hostname = docker
ssh_username = raspi
containers = hass pihole unbound mqtt

[linode]
ssh_username = linode
ssh_hostname = edge.lan.ddnsgeek.com
small_type = g6-nanode-1
big_type = g6-standard-1
linode_name = docker
containers = traefik doods passbolt nextcloud searxng

[firewall]
last_ip = 211.30.131.169
last_change = Sat Jun 08 at 19:02
log_file = /home/linode/docker/update-firewall.log

[rules]
# Define each rule with the format: <protocol>:<port>
rule1 = tcp:22
rule2 = udp:161

[before_rules]
file_path = /etc/ufw/before.rules
input_line = -A ufw-before-input -p icmp --icmp-type echo-request -s
forward_line = -A ufw-before-forward -p icmp --icmp-type echo-request -s