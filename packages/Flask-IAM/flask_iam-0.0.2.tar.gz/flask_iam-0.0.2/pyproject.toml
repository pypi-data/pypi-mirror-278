[build-system]
requires = ["setuptools","setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "Flask-IAM"
version = "0.0.2"
readme = "README.md"
dependencies = [
  'Flask-Login==0.6.3',
  'Flask-UXFab>=0.0.1'
]
