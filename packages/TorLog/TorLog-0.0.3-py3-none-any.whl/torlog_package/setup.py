from setuptools import setup, find_packages

setup(
    name='TorLog',
    version='0.0.1',
    author='Your Name',
    description='Simple logging tool for Python | tore.ofc on discord.',
    url='https://github.com/Tore09/torlog',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
)