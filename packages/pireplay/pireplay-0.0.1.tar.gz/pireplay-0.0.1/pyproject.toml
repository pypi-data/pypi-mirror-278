[tool.poetry]
name = "pireplay"
version = "0.0.1"
description = "Capture and instantly replay the last moments using a Raspberry Pi and a camera with a WEB application."
authors = ["gruvw <gruvw.dev@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.3"
click = "^8.1.7"

[tool.poetry.scripts]
pireplay = "pireplay.cli:run"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
