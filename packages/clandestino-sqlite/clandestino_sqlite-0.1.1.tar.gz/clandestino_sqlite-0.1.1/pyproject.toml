[tool.poetry]
name = "clandestino_sqlite"
version = "0.1.1"
description = "Clandestino SQLite implementation"
authors = ["XimitGaia <im.ximit@gmail.com>"]
readme = "README.md"
packages = [
    {include = "clandestino_sqlite"},
]

[tool.poetry.dependencies]
python = "^3.11"
python-decouple = "^3.8"
clandestino-interfaces = "^0.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
