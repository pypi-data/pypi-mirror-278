[tool.poetry]
name = "turing_data_transformation"
version = "0.1.0"
description = "A library for data transformations in machine learning."
authors = ["Karolis Andrijauskas <karolis.a.and@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.21.0"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"