[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "finn-finder"
authors = [
    {name = "Magnus Kristiansen", email = "magnuskri99@gmail.com"},
]
version = "0.0.0"
description = "An application for advanced filtering of finn.no listings"
classifiers = [
    "Development Status :: 1 - Planning",
]
