[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "finn-scraper"
authors = [
    {name = "Magnus Kristiansen", email = "magnuskri99@gmail.com"},
]
version = "0.0.0"
description = "A web scraper for finn.no"
classifiers = [
    "Development Status :: 1 - Planning",
]
